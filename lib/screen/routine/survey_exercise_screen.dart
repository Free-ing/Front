import 'package:flutter/material.dart';
import 'package:freeing/layout/screen_layout.dart';

class SurveyExerciseScreen extends StatelessWidget {
  const SurveyExerciseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return ScreenLayout(title: 'title', body: Center(child: Text('얗호'),));
  }
}
