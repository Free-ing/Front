import 'package:flutter/material.dart';

class AiLetter extends StatelessWidget {
  const AiLetter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('편지')),
      body: Center(child:Text('편지지')),
    );
  }
}
