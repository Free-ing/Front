import 'package:flutter/material.dart';

class FeedbackListTabbar extends StatelessWidget {
  const FeedbackListTabbar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Column(
        children: [
          Text('문의 내역 조회 화면')
        ],
      ),
    );
  }
}
