import 'package:flutter/material.dart';

class SettingResetPassword extends StatelessWidget {
  const SettingResetPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
